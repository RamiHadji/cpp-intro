// Square Root Function

#include <iostream>
#include <cmath>

using namespace std;


int main() {
  // Declare a variable named x and assign it the value 64
  double x = 64;

  // Use the sqrt() function to calculate the square root of x and print the result to the console
  cout << "Sequare root value of 64 : " << sqrt(x) << endl;

  // Use the sqrt() function to calculate the square root of 50 and print the result to the console
  cout << "Sequare root value of 50 : " << sqrt(50) << endl;

  return 0;
}
